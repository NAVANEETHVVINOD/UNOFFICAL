import ExploreClient from "./ExploreClient";

export const metadata = {
    title: "Explore | Linker",
    description: "Discover clubs, events, and resources across campus.",
};

export default function ExplorePage() {
    return <ExploreClient />;
}
